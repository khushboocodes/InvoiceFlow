# Core extraction pipeline — pinned versions, all PyPI, all open-source

# PDF / image ingestion
PyMuPDF==1.24.13
pdf2image==1.17.0
Pillow==11.0.0

# OCR (multilingual: en + hi + gu)
paddleocr==2.8.1
paddlepaddle==2.6.2

# Vision (signature + stamp detection)
ultralytics==8.3.0

# Local SLM (text-only fallback, safetensors via transformers)
transformers==4.46.3
accelerate==1.2.0
sentencepiece==0.2.0

# Image processing
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Fuzzy matching
rapidfuzz==3.10.1

# Schema & validation
pydantic==2.9.2

# YAML parsing
pyyaml==6.0.2

# Hardware detection (used for CUDA probing only)
torch==2.4.1

# Testing
pytest==8.3.3
